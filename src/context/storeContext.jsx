const { createContext } = require("react");

export default StoreItemsContext = createContext(null); 

function StoreContext() {

  

  return (
    <StoreItemsContext.Provider value={ }></StoreItemsContext.Provider>
  )
  
} 